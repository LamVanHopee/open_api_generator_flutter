name: open_api_generator_flutter
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.6
  built_value: ^8.4.1
  openapi_generator: ^4.0.0
  cupertino_icons: ^1.0.2

  openapi:
    path: ./api

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.2.1

flutter:
  uses-material-design: true
